name: CI

on:
  push:
  pull_request:

jobs:
  container-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run tests in container
        run: tests/container/run.sh --engine docker

