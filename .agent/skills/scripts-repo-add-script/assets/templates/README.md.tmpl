# {{id}}

## What it does

{{one_line}}

## Usage

```bash
scripts/{{id}}/{{id}}.sh --help
```

## Run via curl/wget

curl:

```bash
curl -fsSL https://raw.githubusercontent.com/uv-xiao/scripts/main/scripts/{{id}}/{{id}}.sh | bash -s -- --yes
```

wget:

```bash
wget -qO- https://raw.githubusercontent.com/uv-xiao/scripts/main/scripts/{{id}}/{{id}}.sh | bash -s -- --yes
```

